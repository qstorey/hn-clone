{
  "name": "hn-clone",
  "version": "1.0.0",
  "description": "A Hacker News clone",
  "main": "index.js",
  "scripts": {
    "build": "babel --presets es2015,react src/ --out-dir public/scripts",
    "serve": "simplehttpserver public",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel --watch --presets es2015,react src/ --out-dir public/scripts"
  },
  "author": "Quiran Storey",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "simplehttpserver": "0.0.6"
  }
}
